import { TranslDatePipe } from './transl-date.pipe';

describe('TranslDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslDatePipe();
    expect(pipe).toBeTruthy();
  });
});
